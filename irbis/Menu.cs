using Irbis;
using System;
using System.IO;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using Microsoft.Xna.Framework.Input;

public class Menu
{
    //List<Button> buttonList;
    //string highlightModification;


 //   public Menu(Direction align, Rectangle firstButton, string[] buttonTexts, string highlightMod, bool useBorder, Font font, float depth)
	//{
 //       //align, location, buttonsize (rectangle), number of buttons, 

 //       for (int i = 0; i < buttonTexts.Length; i++)
 //       {
 //           Button tempButton = new Button(new Rectangle(firstButton.X, firstButton.Y, firstButton.Width, firstButton.Height), align, buttonTexts[i], highlightMod + buttonTexts[i].ToUpper(), Color.Magenta, nullTex, font, Color.Magenta, false, false, 1f);
 //           buttonList.Add(tempButton);
 //       }
	//}

    //public Menu(Direction align, Rectangle firstButton, string[] buttonTexts, string highlightMod, Texture2D borderTex, Font font, float depth)
    //{
    //    //align, location, buttonsize (rectangle), number of buttons, 

    //    for (int i = 0; i < buttonTexts.Length; i++)
    //    {
    //        Button tempButton = new Button(new Rectangle(firstButton.X, firstButton.Y, firstButton.Width, firstButton.Height), align, buttonTexts[i], highlightMod + buttonTexts[i].ToUpper(), Color.Magenta, borderTex, font, Color.Magenta, false, false, depth);
    //        buttonList.Add(tempButton);
    //    }
    //}
}
